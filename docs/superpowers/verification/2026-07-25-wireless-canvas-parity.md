# 无线画布对齐验证记录

日期：2026-07-25

对比目标：

- 参考端：`https://meida.sscmsc.com/create/viral-workbench/cw_eafa9fab72bb4f41831f1737543abddd`
- 本地端：`/create/short-drama/storyboard/:projectId?chapter=:chapterId&view=workflow`
- 验证章节：项目 `9`、章节 `2162`

## 最终结果

本地无线画布已经按参考端逐组核对并完成实现。按钮、节点配置、恢复路径、持久化和并发切章均已覆盖；最终浏览器状态无弹窗、无测试便签、无临时水印节点、无临时视频合成节点。

## 自动化验证

| 验证项 | 命令 | 结果 |
| --- | --- | --- |
| 前端单元与组件测试 | `cd web && npm run test` | 29 个测试文件、84 个测试全部通过 |
| 前端类型检查 | `cd web && npm run typecheck` | 通过 |
| 前端生产构建 | `cd web && npm run build` | 通过；1887 个模块完成转换 |
| 后端完整测试 | `uv run pytest` | 186 通过、1 跳过、9 个已接受的非画布基线失败 |

后端的 9 个失败与本次画布修改无关，属于用户已接受的现有基线：过期任务清理、无配置 API、执行器过期状态和 mention 正则返回形状。前端画布相关测试没有失败。

## 逐组对比

| 功能组 | 参考端行为 | 本地验证 |
| --- | --- | --- |
| 页面框架 | 项目标识、状态、画布与视图切换 | 工作流页和全屏工作流均可用；章节标题与状态显示正确 |
| 添加节点 | 添加菜单按节点类型创建 | 菜单项、创建位置、选择状态和持久化已对齐 |
| 导航模式 | 选择、拖动画布、缩放、适配视图 | 工具状态、画布锁定与视口操作可恢复 |
| 编辑工具 | 删除、复制、粘贴、撤销、重做、自动布局、背景分区 | 可用状态随选择变化；删除和历史恢复均已验证 |
| 资产节点 | 类型、昵称、生成数量、尺寸、格式、候选版本和主版本 | 配置、保存和主版本切换已对齐 |
| 镜头节点 | 描述、提示词、时长、比例、分辨率、尾帧、资产输入、模型 | 参数、输入策略和动作能力已对齐 |
| 上传入口 | 图片、视频、音频文件选择 | 三个文件选择入口均可打开和取消；上传状态及媒体节点有测试覆盖 |
| 媒体库 | 资源列表、搜索、选择 | 首次打开立即加载；搜索 0 条后清空恢复 24 条 |
| 图片批注 | 图形、颜色、线宽、撤销、重做、保存 | 工具交互及数据持久化已验证 |
| 水印节点 | 资源、五个位置预设、X/Y、缩放、视频输入 | 五个预设和自定义参数逐项验证；关闭重开、撤销重做和刷新均保留；服务不可用时显示“当前服务未启用水印执行” |
| 视频合成 | 三类输入、有序片段、分辨率、比例、预计时长 | 输入顺序、上移下移、选项、撤销重做和刷新均已验证；服务不可用时显示“当前服务未启用视频合成” |
| 运行能力 | 仅允许实际可执行节点触发 | 集中能力表驱动按钮状态，不伪造输出 |
| 删除结果 | 删除生成结果后节点同步消失 | 自动节点隐藏、边同步过滤，且可撤销和重做 |
| 并发切章 | 后一次章节加载覆盖前一次 | 2163 快速切回 2162 后最终仍为 2162 和“第 1 章节点”；旧请求被取消 |

## 浏览器与视觉证据

- 参考端和本地端均通过用户当前 Chrome 会话逐按钮核对。
- 最终本地刷新后：`dialogs=0`、`notes=0`、`watermark=0`、`composer=0`，页面停留在项目 9 / 章节 2162 / 工作流视图。
- 最终本地宽屏截图为 2555×1278，画布、工具栏、节点连接和小地图正常。
- 留存的页面模式与全屏模式 QA 截图均为 1280×720，并在本轮重新目检。

## 已知边界

- Chrome 扩展没有文件注入权限，因此浏览器内验证到文件选择器打开/取消；成功上传路径由后端上传验证和前端测试覆盖。
- 当前环境未启用水印执行和视频合成服务，因此两类执行按钮按真实能力禁用，并显示明确原因；没有创建伪造结果。
- 没有点击任何可能产生费用的真实生成、合成或水印执行动作。

## 实现提交

关键提交从测试骨架到并发收口依次为：

`327db00`、`b507e3e`、`2e12406`、`5cbccde`、`61c03f1`、`bd0fb93`、`849088f`、`ac8bf71`、`c178ad3`、`7cb5615`、`cf4e95c`、`08c0a1a`、`a29b6d4`、`023fcea`、`ab2ad87`。
