# 创作助手可用性改造与验收

目标：普通用户从小说输入、章节理解、资产提取到分镜生成，再通过自然对话完成图片与分镜提示词修改。以实际操作成功为验收依据，不以测试数量代替体验验收。

## 当前证据

- 用户截图和本机设定页：助手停用仍展示空白聊天区和禁用输入；只有“联系管理员”，没有可操作的恢复入口。
- 资产卡片没有交给助手修改的入口，编辑弹窗和聊天不能顺畅配合。
- 工具栏不换行，助手展开后部分操作超出可视范围；主题按钮覆盖输入区。
- 助手的打开状态、草稿和模型选择在页面切换后丢失；会话只有编号，无法辨认。
- 对象列表混合镜头、资产和形态，没有搜索或明确的本章范围，手动翻找成本高。
- 导入仅支持文件；阶段间下一步文案笼统，失败后恢复路径不足。

## 实施与完成条件

1. **工作区和操作入口**：助手与页面并排且不遮挡；工具栏响应容器宽度；资产和镜头均可一键交给助手；关闭、切换章节或阶段不丢失对话草稿和模型偏好。手机与窄屏可以返回编辑区。
2. **可交互的助手**：启用条件和无模型、无权限、加载失败分别说明；有权限者可走既有配置接口启用；可搜索和调整对象范围；自然描述、发送、停止、重连、新会话、历史、修改结果定位和撤销都能实际操作。
3. **完整创作流程**：文件导入与粘贴正文均可创建项目；展示真实导入/分析/提取/生成进度与失败恢复；每阶段明确下一步及其前提，当前章节在切换和刷新后保持一致。
4. **自然对话和上下文**：会话以首条要求命名；明确当前章和选中目标；修改记录展示可读对象名；连续修改保持人物约束；每个独立镜头编号从 1 开始，完整展开人物描述。助手继续只有图片提示词、分镜提示词两类写入权限，生成操作复用现有流程入口。
5. **验收**：针对上述行为补回归，完成前后端相关测试、类型检查和生产构建；在隔离测试项目中实际走通导入→提取→分镜→多轮修改→查看→撤销→切换会话/章节；在常规桌面、助手展开、窄屏和暗色界面检查操作可见与可达。

## 原则

复用项目现有 Vue 组件、Pinia、XSender/BubbleList、Pydantic AI、后台模型配置与生成服务；遵循 AGENTS.md 和既有 Ponytail 简化原则。不增加第二套 Agent 框架。密钥留在现有配置中，真实用户项目不作为反复修改的验收数据。

## 进度

- [x] 核对当前分支、源码与用户截图，重现设定页操作障碍。
- [x] 工作区和直接修改入口。
- [x] 助手状态、对象选择、草稿与会话体验。
- [x] 导入、提取、分镜各阶段引导与恢复。
- [x] 后端可读会话与修改记录、自然对话验证。
- [x] 端到端操作和响应式视觉验收。


## 实际验收记录（2026-09-09）

隔离项目：`创作体验验收 · 雨夜来信 0909`（项目 22），两章：雨夜车站（5091）、清晨咖啡馆（5092）。通过真实页面粘贴正文、创建项目、识别两章，再逐章提取资产、生成分镜和对话修改。未用真实用户项目反复修改验收。

| 完成条件 | 已核对的实际证据 |
| --- | --- |
| 小说输入与阶段引导 | 粘贴正文创建项目并识别两章；文件来源及章节识别 API 回归通过。创建中禁止重复提交；识别失败保留项目，继续识别前先检查原项目是否已有章节。 |
| 资产提取与直接编辑 | 两章真实提取均完成；人物林夏、车站、咖啡馆和道具可见。资产卡片“用助手修改”直接绑定目标；图片提示词自然修改成功。 |
| 分镜生成 | 第一章任务 `26242849-7d0e-4f51-96e5-60e11df65470`、第二章任务 `96ab7816-fe9f-44f3-aadb-504381558701` 完成。第二章提示词展开了林夏的年龄、发型、风衣与单肩包。 |
| 自然多轮对话及跨会话记忆 | 图片修改 `d4b35861-6761-4018-b702-9449a11752ca` 成功保存全书约束；新会话修改 `cc16030d-3d29-4c56-a29d-45112bcc5647` 完成，沿用既有林夏设定。 |
| 跨章范围隔离 | 第二章修改 `9911a249-86b9-4f80-a38f-1d28ad9bf917` 完成，使用清晨自然光并保留人物，不覆盖第一章暖灯；作用范围回归覆盖章节、剧情区间、人物和旧镜头局部规则。 |
| 每个请求独立完整 | 多分镜任务 `0d4e5ca6-08ff-4388-8b30-bef907cadff0` 成功，只修改分镜 79、80。两者内部标题均为 1、2、3，总时长分别 8 秒，均包含完整人物外貌。模型请求累计约 37 秒。 |
| 记录、定位、撤销、停止 | 从图片修改记录定位到林夏编辑界面；修改记录 2 实际撤销并恢复内容；视频参数变更不再错误阻止提示词撤销。停止任务 `9d8aa653-32c6-435b-9163-5c501187b5f8` 终态为取消，无新增修改。 |
| 草稿、模型和历史 | 关闭重开保留草稿；新会话与历史可切换；阶段及章节切换保留会话和模型；切换会话使用其独立草稿。长对话打开后滚动到最新回复。 |
| 响应式与外观 | 实际检查常规桌面、1024×768、390×844、浅色和深色。助手桌面并排，手机可关闭返回编辑区；模型位于输入框下方；主题按钮移入页头；长目标名称缩略，不挤占聊天区。 |
| 失败恢复 | 实测模型输出截断、上下文与总量额度不足，并据此修复错误提示；失败轮次和已保存结果独立显示。分镜断网回归证明保留原任务、重新连接不重复生成。 |

### 运行配置与费用

使用既有 Pydantic AI、AG-UI、XSender、BubbleList、Pinia 和项目组件，没有引入新框架、依赖或锁文件修改。助手仍仅有图片提示词与分镜提示词两个业务写入工具。新分镜生成复用既有生成入口，并读取保存的创作约束。

真实 DeepSeek 配置原先采用模型默认思考，曾耗尽 3,000 / 8,000 输出额度而没有产出修改。通过既有后台将思考模式设为关闭后，多分镜修改完成。此设置作用于使用该模型配置的新任务。当前助手配置为上下文 64,000 字符、单次输出 8,000、整轮总量 100,000；调用数 6、工具调用数 8、目标数 8、超时 180 秒，均由后台管理。新安装的默认额度同步为上述值，既有显式配置不被静默覆盖。

测试项目账本已记录约 4.00 元，包含分析、提取、分镜和失败重试；一个失败请求缺少供应商用量返回，因此该数字是已记录费用，不视为完整供应商账单。旧评估费用账本未改动。

图片服务的 IP 白名单限制仍使封面请求失败；分析结果现在先保存，封面失败显示可理解的提示，用户可继续提取资产和制作分镜。没有改动供应商安全限制，也没有进行视频生成。

### 回归依据

- 前端全量 503 项测试通过（145 个文件），助手/工作区定向回归、类型检查及生产构建通过；包含输入框真正清空、草稿切换、已接受提交后历史读取失败、断线重连、导入恢复。
- 后端相关 136 项回归通过；随后错误分类回归通过；鉴权模式单独 5 项通过；文件来源和分章 API 3 项通过。
- `git diff --check`；未修改或提交密钥、数据库、媒体、日志、构建输出、依赖与锁文件。

## 普通用户操作路径

1. 创建 Agent 项目，上传书稿或粘贴正文。
2. 剧本页检查正文与设定，点击“前往资产提取”。
3. 点击“提取本章资产”，在人物或场景卡片上点“用助手修改”，直接描述希望改变的画面。
4. 点击“前往分镜制作”，生成本章分镜，再从分镜卡片选择“用助手修改”。多个对象可在助手的修改范围中一起勾选。
5. 直接说“光线更柔和，保持人物外貌”；持续要求可说“全书保持……”。结果会保存到对应提示词，记录中可以查看差异、定位和撤销。
6. 模型选择在输入框下方，新会话和历史在上方。停止仅停止当前运行，已经保存的结果仍在记录中。
